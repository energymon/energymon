# Dummy Energy Monitor

This is a dummy implementation of the `energymon` interface.

## Usage

To link with the shared object library:

```
-lenergymon-dummy
```

To link with the static library version:

```
-lenergymon-dummy-static
```
